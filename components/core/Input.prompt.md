Underlined text field for forms — the brass underline animates in on focus. No boxed border, by design.

```jsx
<Input label="Name" placeholder="Ihr Name" />
<Input label="E-Mail" type="email" required onDark />
```

Use `onDark` inside ink sections. Stack several with `gap: var(--space-6)` for a contact form.
